# Data folder

This repository does not bundle the original horse colic dataset files.

To run the project, place the following files here:

- `horseColicTraining2.txt`
- `horseColicTest2.txt`

The cleaned pipeline script expects whitespace-separated text files with the same schema as the original coursework dataset.
